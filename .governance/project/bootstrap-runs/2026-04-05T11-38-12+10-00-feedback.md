# Bootstrap Update Feedback

Run timestamp: `2026-04-05T11:38:12+10:00`

## Summary
Update mode substantially tightened the bootstrap contract compared with the earlier repo state: it forced explicit repo-state classification, a commit-policy declaration, strict Git workflow artifacts, GitHub preflight reporting, canonical board normalization, and final live-state reconciliation. The contract was useful, but it still leaves one operational gray area around private-repo branch-protection capability because the platform can block API verification even when repo admin access exists.

## What worked well
- The live canonical sources were explicit enough to drive a stronger update pass than the earlier scaffold.
- The empty-repo fallback for `SPEC-001` was clear enough to replace guessed product scope with a bootstrap/setup spec.
- The GitHub board contract was concrete enough to create and normalize a canonical board with required fields.

## What still felt rough
- Branch-protection verification mixes feature availability and admin capability into one check, which can make a repo look half-configured even when the actor has admin rights.
- Dirty-repo handling is explicit, but it would help to give a named example for normalizing pre-existing bootstrap artifacts rather than forcing the operator to infer that this counts as "resolve first."
- The contract expects durable local run artifacts, but it does not prescribe a compact template for the status file, so implementers still have to invent structure.

## Useful follow-up improvement
- Add a small canonical status-artifact template to the bootstrap page with sections for starting state, preflight outcomes, canonical board, blockers, final reconciliation, and settled end-state classification.
