# Bootstrap Update Feedback

Run timestamp: `2026-04-05T14:56:09+10:00`

## Summary
This rerun clarified two useful things in the live contract: every run should have a timestamped analysis artifact, and private-repo branch-protection limits should be framed as degraded verification instead of generic failure. That makes update-mode results easier to report honestly when the repo is otherwise normalized.

## What worked well
- The live canonical sources were stable enough that this rerun mostly confirmed the committed bootstrap state instead of forcing more repairs.
- The added `analysis` artifact gives a clearer place to explain how the run interpreted dirty state, live-state reconciliation, and degraded verification.
- The board normalization remains durable across reruns.

## What still feels rough
- The hosted-plan/private-repo limitation still requires some interpretation because `bootstrap.json` outcome states are simpler than the Bootstrap page’s degraded-verification nuance.
- Update-mode reruns produce a lot of timestamped history quickly; the docs could say more about whether all reruns should always be kept or whether some can be compacted later.

## Useful follow-up improvement
- Add one canonical example showing a dirty-start update rerun that preserves valid prior bootstrap artifacts, adds any newly required artifact types, and reports degraded verification without recasting the whole bootstrap as failed.
