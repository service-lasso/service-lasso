# Operator MCP

## Intent
Give Cursor and other MCP clients a local-first operator surface on the Service Lasso runtime: inspect services, secret *metadata*, logs, and later guarded lifecycle actions through the same facades Service Admin uses. This spec binds the production MCP programme already tracked as GitHub `#858`–`#864` and the product leftover `#1067`. It does not invent a second MCP server or a second secrets product.

## Scope
Included:
- One MCP adapter in `service-lasso` that reuses runtime/operator facades
- Read-only tools and resources, including secret metadata without values
- A Streamable HTTP identity slice with OAuth protected-resource discovery, scopes, explicit transport modes, cumulative permission profiles, per-actor/client rate limits, and fail-closed server/Audit policy under `#860` / `AC-6C`
- Complete strict read tools and resources under `#861`, guarded lifecycle and maintenance actions under `#862`, followed by long-running operations and release gates
- Documentation in `docs/reference/operator-mcp.md`

Explicitly out of scope:
- A parallel MCP server in Service Admin, Secrets Broker, or work-agents
- Secret values, tokens, cookies, private keys, recovery material, raw env, or KV get/reveal in MCP output
- Generic shell, unrestricted filesystem, raw config documents, or stdin tools
- Guessing a new transport/identity architecture beyond `#858` / `docs/reference/operator-mcp.md`
- Service Admin MCP settings UI (`lasso-serviceadmin#423` is already closed)
- Canonical demo recycle or keep-alive ownership from this spec
- The inherited `#1067` missing SDK-registration baseline defect; `#860` must keep its identity boundary independently testable

## Acceptance Criteria
- `AC-6A`: The read-only MCP prototype exposes a dedicated secret-metadata tool and resource that return allowlisted metadata only: secret refs, namespace/key, assignment/access-policy status, rotation readiness, and Secrets Broker lifecycle availability. Responses, resources, and tests never include secret values, credential sentinels, env values, tokens, cookies, private keys, recovery material, or absolute workspace/manifest/log paths. Live broker lockout counts stay on the existing telemetry bridge until a later slice; this criterion reports lockout as not queried rather than fetching KV.
- `AC-6B`: MCP protocol plumbing uses the supported official TypeScript SDK with stdio and standards-compliant Streamable HTTP (`#859`). This criterion is not satisfied by handwritten JSON-RPC alone.
- `AC-6C`: Streamable HTTP identity, OAuth protected-resource discovery, scopes, explicit disabled/read-only/guarded modes, per-actor/client rate limits, and server-side policy fail closed (`#860`). Observer credentials cannot mutate state. Permission profiles are derived from cumulative granted scopes, actors come from validated identity rather than tool arguments, and MCP execution stops safely when its Audit event cannot be persisted. Local stdio uses an opt-in credential-bearing adapter connected to the active runtime directly, without starting a competing process or looping back over HTTP.
- `AC-6D`: The complete read-only operator surface covers runtime status, paginated services, health, routes, dependencies, bounded redacted logs, Audit search, updates, drift, recovery, and operations, with strict schemas and structured output (`#861`). `#1067` closes only when this criterion plus `AC-6G` meet the product bar for services, secret metadata, and logs.
- `AC-6E`: Guarded lifecycle and maintenance tools call the shared operator/application facade without HTTP loopback or duplicated lifecycle logic; perform metadata-only preflight; enforce the Observer/Operator/Maintainer/Administrator matrix; and require expiring, actor-, target-, parameter-, plan-, candidate-revision-, and single-use server confirmation for risky actions (`#862`). Confirmed plans bind endpoint allocations, manifest definitions, template bytes, resolved service/provider launch files, recursive setup-step, doctor, stop-override and update-hook execution files, and exact SHA-256 artifact/update candidates, then revalidate those bindings at the relevant materialization or immediate pre-spawn boundary. Launch evidence uses the supervisor's exact resolver, including basename and option-file inputs. Update installation rehashes confirmed archive bytes after pre-upgrade hooks and extracts only the verified in-memory bytes; its confirmation states only the stop and hook subprocess effects executable under the chosen force/policy path. Exact duplicate idempotency keys are claimed across runtime processes and replay the recorded safe result with truthful replay metadata without repeating mutation, while altered or token-like reuse fails closed. Synchronous completions return bounded resulting lifecycle state. Every allowed, schema-denied, policy-denied, skipped, failed, and replayed attempt writes durable Audit with a safe correlation id; terminal Audit outages retain a recoverable pending outcome rather than repeating a mutation. MCP exposes no generic shell, terminal/stdin, raw filesystem, raw configuration, or secret-value action.
- `AC-6F`: Confirmed long-running install and update actions that do not complete within the bounded MCP request budget return a durable opaque operation id without duplicating the underlying guarded action (`#863`). Operation get/list output is actor- and workspace-scoped by default; only Administrator authority can inspect or cancel another actor's operation. Records survive MCP client disconnect and runtime restart whenever the underlying runtime action survives, and expose only strict status, phase, numeric progress, safe summary, timestamps, allowlisted target ids, correlation id, cancellation support, and terminal outcome. Supported cancellation reaches a deterministic terminal state through the shared facade and records durable Audit; unsupported or too-late cancellation is an explicit non-destructive result. MCP request cancellation is propagated where safe without orphaning or repeating the action. Completed operations have documented bounded retention and cleanup. The domain model stays independent of experimental MCP Tasks so a later Tasks adapter does not change launch behavior. No operation state contains raw command output, logs, secrets, credentials, config bodies, absolute paths, or unbounded error text.
- `AC-6G`: Security, redaction, protocol conformance, packaging, and canonical demo acceptance are blocking product gates in the normal CI/release path (`#864`). The gate drives the supported official MCP Inspector and SDK client against Streamable HTTP, proves the opt-in stdio adapter, initialization/version negotiation/notifications/discovery, closed-world schemas, OAuth identity and transport defences, independent actor/client rate limits, the complete permission/confirmation/idempotency/operation/Audit matrix, deterministic limits, and sensitive-output rejection. A staged package is installed into a fresh consumer root and starts the same MCP server without a source checkout on Windows, Linux, and macOS. Canonical acceptance performs safe discovery and representative reads plus one confirmed exactly-once lifecycle action. Each operating-system lane retains one nonempty, exact-SHA-bound, metadata-only evidence artifact containing supported protocol/SDK/Inspector versions and representative result classifications, with no protocol captures, credentials, raw logs, configuration, environment values, local paths, or secret material.

## Tests and Evidence
- Focused `tests/operator-mcp.test.js` coverage for tool/resource advertisement, secret-metadata shape, unknown-service errors, extra-argument rejection, and `assertNoSecretMaterial` redaction.
- Focused `#860` coverage for OAuth discovery, Origin/content boundaries, token validation, scope denial, trusted actor derivation, explicit modes, cumulative permission profiles, independent actor/client rate limits, deterministic Audit-store failure, and redacted failures.
- `tests/operator-mcp-read-surface.test.js` proves deterministic pagination, allowlisted output shaping, stable errors, schema/annotation contracts and secret/config/token/path regression sentinels for `#861` / `AC-6D`.
- `tests/operator-mcp-guarded-actions.test.js` proves the complete permission matrix, exact preflight targets/effects, confirmation expiry/alteration/replay/actor/target/parameter binding, shared launch-root/basename evidence, launch/setup/template/doctor/stop-hook/update-hook byte-alteration denial, post-hook immutable update-archive revalidation, a successful guarded install/restart, exactly-once idempotency, shared-facade lifecycle effects, durable Audit correlation, and sensitive-output rejection for `#862` / `AC-6E`.
- `tests/operator-mcp-operations.test.js` proves durable install/update polling, supported and unsupported/too-late cancellation, actor/workspace isolation plus Administrator override, client disconnect without orphaning or duplicate execution, runtime restart rehydration, bounded retention/cleanup, Audit start/cancellation/final outcomes, and sensitive-output rejection for `#863` / `AC-6F`.
- `tests/operator-mcp-product-acceptance.test.js` aggregates the supported Inspector/SDK protocol, stdio, identity, permission, confirmation, idempotency, durable-operation, pagination, Audit and leakage contracts for `#864` / `AC-6G` without weakening their focused owning suites.
- The complete repository suite keeps Windows process fixtures deterministic under slow host identity inspection: intentional short-lived exits are triggered only after ownership is committed, and owned fixture processes receive a bounded finalization deadline before temporary roots are removed. Production process-ownership checks remain fail-closed.
- `scripts/verify-mcp-packaged.mjs` stages and installs the package into a fresh consumer root, launches the packaged runtime and acceptance driver from that isolated consumer working directory with consumer-only module resolution, drives every advertised protocol revision plus Streamable HTTP, Inspector and stdio through public entrypoints, performs the canonical safe read/confirmed exactly-once guarded-action scenario, and writes one recursively closed metadata-only evidence record per operating system containing only directly executed packaged classifications. Windows identity inspection uses one process handle across real `OpenProcess`, `GetProcessId`, `GetProcessTimes`, `QueryFullProcessImageNameW`, and bounded `NtQueryInformationProcess` command-line evidence; it hashes and discards the command line without changing the four-field fingerprint. Nonzero helper exit, access denial, timeout, empty/malformed output, or partial evidence is unknown ownership, while only an explicit successful absent-process result is not running. Packaged HTTP and stdio exercise the unchanged 15-second product deadline with no timeout override or injected identity. Closed evidence records `native-win32-product-default` for Windows and `product-default` elsewhere.
- `.github/workflows/mcp-product-acceptance.yml` runs the source conformance and fresh-consumer packaged acceptance on Windows, Linux and macOS, uploads the three retained artifacts, and requires an aggregate exact-SHA/artifact readback before success.
- `#860` includes local stdio credential handling and smoke evidence. The adapter must reuse the active runtime directly; a second runtime owner or HTTP-loopback adapter would violate the architecture.

## Documentation Impact
- `.governance/specs/SPEC-006-operator-mcp.md`
- `docs/reference/operator-mcp.md`
- `.governance/project/BACKLOG.md` MCP issue register
- `.governance/project/PROJECT_INTENT.md`

## Verification
- `npm run build`
- `node --test --test-concurrency=1 tests/operator-mcp-identity.test.js` for `#860` / `AC-6C`
- `node --test --test-concurrency=1 tests/operator-mcp.test.js` for the inherited read-only MCP surface
- `node --test tests/operator-mcp-read-surface.test.js` for strict `#861` read contracts and sensitive-output regression coverage
- `node --test --test-concurrency=1 tests/operator-mcp-guarded-actions.test.js` for `#862` / `AC-6E`
- `node --test --test-concurrency=1 tests/operator-mcp-operations.test.js` for `#863` / `AC-6F`
- `node --test --test-concurrency=1 tests/operator-mcp-product-acceptance.test.js` for the blocking source conformance matrix under `#864` / `AC-6G`
- `node scripts/verify-mcp-packaged.mjs` for fresh-consumer packaged and canonical acceptance
- No live tokens or secret values in fixtures, logs, or MCP responses

## Change Notes
- 2026-08-30: Exact candidate `ce0e550be17ccf6b306d143f63a973d4fefebf6c` passed local packaged verification, the complete 45-test MCP source gate, Linux/macOS hosted packages, and independent source review, but was held after both hosted Windows package lanes remained unable to complete `Get-CimInstance` even with the capped 60-second acceptance allowance. MCP Product Acceptance `33280153840` failed during canonical guarded preflight; Release Qualification `33280153810` failed during workspace-lock owner inspection, and each run retained only Linux/macOS MCP evidence. A direct WMI trial at `22978e54162e054bd4ae2fa7838daa7fb40a7549` was rejected locally before push because it still depended on the provider, retained timeout-inflated acceptance, and exposed an older helper-exit/empty-output ambiguity. The active candidate instead uses a single native process handle, explicitly classifies helper and output failures as unknown, and requires packaged Windows proof under the unchanged 15-second product default.
- 2026-08-30: Corrective head `821bfd95ea20e0ab588d70f00bb2e6bd3ce1001e` was held after exact-head CI exposed three harness defects: macOS Node startup crossed the `/var` to `/private/var` temporary-root symlink under the permission model, hosted Windows cold CIM startup exceeded the unchanged 15-second production identity deadline, and Release Qualification plus both publication paths named the evidence file outside the closed artifact verifier contract. Head `cb7fd06e8d72288c6a4e2cc90c70a02e367bdf75` fixed the workflow filenames but proved that merely allowing `/var` does not repair Node main-module canonicalization and that a host-level CIM warmup does not populate the packaged process. Head `ed111084a620a661d08d68883540a9d826bcfa3f` fixed macOS and preserved a controlled Windows system-module path, but each new PowerShell process still exceeded the product default. Local packaged verification at `219c730df336e456a29572c00c5d8c0a5e656143` passed after priming and caching current-process identity, while a wider adopted-child restart test still failed at uncached child inspection. Independent review therefore narrowed the candidate: revert the cross-subsystem identity cache and use an explicitly gated, capped 60-second Windows acceptance default across real inspections, with precedence/reset tests and a truthful closed evidence classification; ordinary product behavior remains unchanged.
- 2026-08-30: Independent review of the first `#864` candidate rejected four evidence shortcuts despite green CI: a skipped canonical action, source-checkout working directories, an open/inflated retained record, and source-only release qualification. The corrective candidate replaces them with confirmed restart execution plus identical replay, a consumer-owned runner with isolated module resolution and stdio cwd, an exact negative-tested evidence contract, every advertised protocol revision plus unsupported fallback, and three-OS fresh-package gates that block Release Qualification, npm publication, and GitHub release publication before mutation.
- 2026-08-30: Local full-suite qualification for `#864` exposed five Windows fixture defects under slow host process inspection: two fixtures exited before secure ownership persistence and three removed temporary service roots before owned-process finalization. The tests now trigger intentional exits after successful start and wait through a bounded Windows convergence deadline; the five formerly failing cases pass together without weakening runtime ownership policy.
- 2026-08-30: `#864` / `AC-6G` begins from exact merged and qualified `#863` corrective SHA `66d3bde19265fc837388fd11d346c29154c6f1d1`. The release gate owns official Inspector/SDK conformance, complete security-regression aggregation, fresh-consumer three-OS packaged MCP startup, canonical safe reads plus one confirmed lifecycle action, and exact-SHA metadata-only retained evidence. It extends the existing MCP server and public package entrypoints; it does not create a second server, publish a release, or treat source-only tests as packaged proof.
- 2026-08-30: `#863` / `AC-6F` completed through primary PR `#1161` and corrective PR `#1162`; exact-merge Release Qualification `33257608278` and three-OS Packaged Admin `33257648540` passed on `66d3bde19265fc837388fd11d346c29154c6f1d1` with retained durable-operation evidence and Project closure readback.
- 2026-08-29: `#863` / `AC-6F` begins from exact merged and qualified `#862` SHA `1cb844a11dd02f25226c8e6124d9146e6d026095`. The durable domain model remains MCP-Tasks-independent, derives actor/workspace authority from validated identity, reuses the guarded shared facade without duplicate mutation, persists only allowlisted safe operation fields, integrates bounded request cancellation, and owns Audit plus cleanup. Product conformance, packaged Inspector, and canonical acceptance remain owned by `#864`.
- 2026-08-29: `#862` / `AC-6E` merged through PR `#1160`; exact-merge Release Qualification `33237947803` and Packaged Admin `33237947802` attempt 2 passed with retained artifacts and Project closure readback.
- 2026-08-29: `#862` / `AC-6E` begins from merged `#861` on the existing SDK server. The action contract is preflight-first, uses the shared application facade, derives authority from validated MCP identity, requires bound single-use server confirmation for risky mutations, records safe durable Audit, and preserves exactly-once behavior for duplicate idempotency keys. Durable operation records remain owned by `#863`; product conformance, packaged three-OS, Inspector, and canonical acceptance remain owned by `#864`.
- 2026-08-25: `#861` completes the read-only inventory, detail, health, routes/Traefik, dependencies, logs, Audit, updates, config drift and recovery contracts with strict schemas, structured content, deterministic cursors, stable errors and path/value-safe allowlists. Operation status reports `feature_unavailable` until `#863` owns durable operation state.
- 2026-08-25: `#860` / PR `#1137` is merged at `52884d4`, completing OAuth/policy plus the credential-gated active-runtime stdio adapter.
- 2026-08-25: `#860` adds an opt-in local stdio credential path and smoke proof through the active runtime process. A protected environment capability plus bounded actor/client ids gate the SDK transport; the secret is neither protocol nor Audit data, while a safe startup Audit event records only derived identity metadata.
- 2026-08-25: `#860` extends the existing Streamable HTTP slice with explicit disabled/read-only/guarded modes, a fail-closed read-only tool allowlist, cumulative Observer/Operator/Maintainer/Administrator classification, independent actor/client rate limits, safe `429` denial Audit evidence, and deterministic fail-closed Audit-store coverage.
- 2026-08-25: `#859` / PR `#1029` is merged. `#860` begins a partial `AC-6C` slice for the existing SDK Streamable HTTP transport while explicitly excluding `#861`–`#863` and the inherited `#1067` registration defect.
- 2026-08-21: `#1067` / `AC-6A` adds `service_lasso_secret_metadata` and `servicelasso://secret-metadata` on the existing handwritten MCP prototype. `#859` draft PR `#1029` remains the transport owner; this slice does not replace JSON-RPC or add OAuth.
- Live Secrets Broker lockout aggregates remain outside the supported Core MCP boundary so MCP does not grow a second KV or telemetry client; any future extension requires a separate governed owner and must preserve the current no-value contract.
