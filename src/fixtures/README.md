# Runtime fixtures

This folder is reserved for the first tracked fixture/sample service definitions used by the core runtime smoke checks.

It is intentionally documentation-only in `TASK-006`.

Planned next use:
- `TASK-008` will add canonical `service.json` discovery/parsing fixtures.
- `TASK-009` will add direct runnable smoke verification against those fixtures.
