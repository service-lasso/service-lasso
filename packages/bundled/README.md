# `@service-lasso/service-lasso-bundled`

This is a bounded placeholder for the future bundled-distribution template.

It intentionally depends on `@service-lasso/service-lasso` so the package boundary is explicit before the real packaged starter template is built out.
