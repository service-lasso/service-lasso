# `@service-lasso/service-lasso-app-web`

This is a bounded placeholder for the future web reference-app template.

It intentionally depends on `@service-lasso/service-lasso` so the package boundary stays explicit while the real starter-template implementation is still queued.
