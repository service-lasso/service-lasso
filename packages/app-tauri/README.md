# `@service-lasso/service-lasso-app-tauri`

This is a bounded placeholder for the future Tauri reference-app template.

It intentionally depends on `@service-lasso/service-lasso` so the package boundary stays explicit before the real template implementation is introduced.
